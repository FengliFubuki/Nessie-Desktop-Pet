# 鳍肢修复

定位采用完整 contact sheet 的零起始编号：ROW6/C1–C4、ROW7/C2、ROW8/C1–C4。

已重新生成 waiting、running、review 三行，共18帧。每帧保留三支可见的单片叶形鳍，第四支被身体遮挡；不再新增腹部手、抬起与落地的重复鳍、拇指或分叉。动作通过头、颈和眼睛表达。

重新提取并组装8×11 atlas，进行一次新版本最终边缘去色键处理，验证1536×2288、v2、透明背景、非空使用格及透明未使用格。动画预览直接裁切自清理后的最终atlas。

三名隔离方向盲测检查者均已完成。上下左右基准方向通过，三个中间方向的轻微轴向不确定已结合带标签的方向环和连续性证据复核接受。

## 头部颜色和 GIF 修复

ROW1、ROW2 的移动动画已重做浅绿色头部，移除头顶宽深色阴影。左向行在完整提取、注册后逐帧镜像自右向行，避免原始条带的等分边界切断尾巴，保留原始帧序和时长；其他九行逐像素保持一致。

全部九个 GIF 直接从最终清理后的 atlas 裁切重新导出，统一每个动画的调色板，保留透明色索引，关闭抖色，使用 128 的 alpha 阈值。实际解码逐帧验证蓝色残留为 0、透明遮罩误差为 0，帧数与时长正确。另附保留完整半透明边缘的无损动画 WebP。
